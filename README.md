# dluau
> work-in-progress
## to build
### dependencies
### build from source:
```sh
cmake --preset default
cmake --build --preset cli --config Release
cmake --build --preset lib --config Release
```
